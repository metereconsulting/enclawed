apiVersion: v1
kind: Service
metadata:
  name: enclawed
  labels:
    app: enclawed
spec:
  type: ClusterIP
  selector:
    app: enclawed
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
