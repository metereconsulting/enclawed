<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_ENCLAWED"
        app:queryPatterns="@array/ask_enclawed_query_patterns">
        <intent
            android:action="ai.enclawed.app.action.ASK_ENCLAWED"
            android:targetPackage="ai.enclawed.app"
            android:targetClass="ai.enclawed.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
