---
summary: "CLI reference for `enclawed clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `enclawed clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `enclawed clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `enclawed clawbot qr` (same behavior as [`enclawed qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `enclawed clawbot qr` -> `enclawed qr`
