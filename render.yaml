services:
  - type: web
    name: openclaw
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: ENCLAWED_GATEWAY_PORT
        value: "8080"
      - key: ENCLAWED_STATE_DIR
        value: /data/.openclaw
      - key: ENCLAWED_WORKSPACE_DIR
        value: /data/workspace
      - key: ENCLAWED_GATEWAY_TOKEN
        generateValue: true
    disk:
      name: openclaw-data
      mountPath: /data
      sizeGB: 1
